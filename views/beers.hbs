<h3>beers</h3>

    {{#each beersFromApi}}
    <div class="beer-cont">
        <img src="{{image_url}}">
        <h3>{{name}}</h3>
        <p>{{tagline}}</p>
        <p>{{description}}</p>
    </div>
    
    {{/each}}
